# Attributions and licenses

No fixture is copied into the corpus without provenance. The checked-out
source directories retain the original repository files and license notices.

* `xapi-js`: MIT, copyright Robert Soriano (see `xapi-js/LICENSE`).
* `xapi`: Apache License 2.0 (see `xapi/README.md`, section License).
* `xplatform-xml`: the pinned checkout contains no `LICENSE`, `COPYING`, or
  `NOTICE` file. Derived wire vectors retain the exact commit, path, and
  `upstream-no-license-file` marker; no license is inferred for that source.
  Redistribution policy for additional copied fixtures requires upstream
  clarification.

This repository's new protocol and runner code is licensed separately by the
repository owner. Provenance is metadata, not a dependency on upstream DTOs.
